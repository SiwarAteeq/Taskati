name: taskati
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  date_picker_timeline: ^1.2.6
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  image_picker: ^1.0.8
  intl: ^0.19.0
  lottie: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  
  assets:
    - assets/images/
 

